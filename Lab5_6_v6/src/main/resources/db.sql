CREATE TABLE student_result (
     id serial primary key not null ,
     name varchar(30),
     second_name varchar(30),
     last_name varchar(30),
     subject varchar(100),
     result varchar(1)
);